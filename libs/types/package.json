{
  "name": "@medix/types",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup",
    "build:dev": "pnpm build --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "mongoose": "^6.8.3",
    "mongoose-paginate-v2": "^1.7.1"
  }
}